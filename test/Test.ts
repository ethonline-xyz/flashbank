import {ethers} from "@nomiclabs/buidler";
import chai from "chai";
import {deployContract, solidity} from "ethereum-waffle";

chai.use(solidity);
const {expect} = chai;

describe("", () => {
  beforeEach(async () => {});

  describe("Testing Flashloan", async () => {
    it("Testing ERC20 flashloan", async () => {});
    it("Testing ETH flashloan", async () => {});
  });

  describe("Testing Deposits", async () => {
    it("Testing ERC20 deposit", async () => {});
    it("Testing ETH deposit", async () => {});
  });

  describe("Testing Withdraws", async () => {
    it("Testing ERC20 withdraw", async () => {});
    it("Testing ETH withdraw", async () => {});
  });
});
